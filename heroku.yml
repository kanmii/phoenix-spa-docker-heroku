setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: db
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - POOL_SIZE=2 mix ecto.migrate
run:
  web: /usr/local/bin/entrypoint.sh
